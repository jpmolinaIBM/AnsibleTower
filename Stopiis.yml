- hosts:
    - all
  tasks:
    - name: Stop {{ app_pool_name }} Website
      win_iis_website:
        name: "{{ app_pool_name }}"
        state: stopped
        port: 80
      register: website
